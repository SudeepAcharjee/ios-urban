workflows:
  ios-workflow:
    name: iOS Workflow

    environment:
      flutter: stable

      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.sudeepacharjee.urbanservice

    scripts:
      - flutter config --no-enable-swift-package-manager
      - flutter pub get
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa